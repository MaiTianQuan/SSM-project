```
git init        (初始化)                                                             
git remote add origin git@github.com:MaiTianQuan/test01.git （本地和远程库关联）
git pull --rebase origin master    (先将远程库初始化到本地，本地会有一个readme.md文件)
git push -u origin master  （将本地内容上传到github,如果远程库是空的）
git push  origin master  （将本地内容上传到github）
git push  origin master  （将本地内容上传到github）
```

```
小米笔记本ssh key所在位置
C:\Users\jinma\.ssh
```
